# Redis 访问密码 (必须与 config.toml 中保持一致或在此统一替换)
REDIS_PASSWORD=123456

# OpenAI 备用 Provider 环境变量 (对应 config.toml 中的 ${OPENAI_API_KEY})
OPENAI_API_KEY=xxxxxxxxxxx

# 阿里云 DashScope Rerank / Embedding 的 API Key (如果 config.toml 中配置为 ${DASHSCOPE_API_KEY})
DASHSCOPE_API_KEY=xxxxxxxxxxxxxxxxx
